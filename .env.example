# Server-side KLONA memory stack configuration.
# Copy to .env with scripts/init_memory_stack.py or by hand. Do not commit .env.

# Host markdown vault path. Mounted only by the low-level memory-server service.
HOST_VAULT_DIR=./vault

# High-level user-agent MCP server (recall/remember tools).
HIGH_LEVEL_MCP_HOST_PORT=32310
# Empty disables auth. Set a private value to require Bearer auth,
# especially when publishing this service beyond localhost.
HIGH_LEVEL_MCP_AUTH_TOKEN=
# Empty disables general MCP Host-header allowlisting, but the admin dashboard
# still only accepts loopback Host headers unless external hosts are listed here.
# Set a comma-separated allowlist of every host clients will use, for example:
# localhost,127.0.0.1,klona.example.com,klona.example.com:32310,203.0.113.10,203.0.113.10:32310
HIGH_LEVEL_ALLOWED_HOSTS=

# REM sleep maintenance. Automatic REM jobs are appended to the FIFO queue after
# this many successful remember jobs. Set enabled=false or threshold <=0 to
# disable automatic REM sleep; the /dashboard manual action still works.
KLONA_REM_SLEEP_ENABLED=true
KLONA_REM_SLEEP_REMEMBER_THRESHOLD=10
