# DEPRECATED — legacy local OpenCode subagent asset

This file is retained only as a historical/cleanup marker. It is not installed by
the OpenCode installer and is not the active KLONA memory-agent implementation.

Use the server-side `memory-agent` service and high-level `recall(input: str)` /
`remember(input: str)` MCP tools instead.
