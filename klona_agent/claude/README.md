# KLONA Claude Integration

Claude integration is not included in Phase 1.

Phase 1 provides:

- the self-contained memory server in `memory_server/`;
- the OpenCode installer in `klona_agent/opencode/`;
- the root installer router in `install_agent.py`.

A future phase may add a Claude installer and Claude-specific assets after the public OpenCode and memory server migration is complete.
